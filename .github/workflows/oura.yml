name: Oura Price Bot

on:
  workflow_dispatch:
  schedule:
    - cron: "*/5 * * * *"

permissions:
  contents: write

concurrency:
  group: oura-price-bot
  cancel-in-progress: true

jobs:
  check-prices:
    runs-on: ubuntu-latest
    timeout-minutes: 4

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install packages
        run: |
          python -m pip install --upgrade pip
          python -m pip install requests beautifulsoup4
          python -c "import requests; import bs4; print('Packages installed successfully')"

      - name: Search for Gold Oura Ring 5
        env:
          NTFY_TOPIC: ${{ secrets.NTFY_TOPIC }}
        run: python oura_bot.py

      - name: Save alerted deals
        run: |
          if [ -f seen_deals.json ]; then
            git config user.name "oura-price-bot"
            git config user.email "oura-price-bot@users.noreply.github.com"
            git add seen_deals.json
            git diff --cached --quiet || git commit -m "Update alerted Oura deals"
            git push || true
          fi
